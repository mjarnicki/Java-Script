'use strict';
function stringReverse(input)
{
    var splited = input.split("");
    var reversed = splited.reverse();
    var joined = reversed.join("");
    return joined;
}
var toReverse = "Akademia108";

